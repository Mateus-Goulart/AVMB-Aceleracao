namespace Vetores___Atividade_5
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int[] numeros = new int[80];
            int i, menor=0, p=0;

            for (i=0; )

            for (i=0; i<5; i++)
            {
                menor = numeros[1];
                if (menor > numeros[i])
                {
                    menor = numeros[i];
                    Console.WriteLine("O menor valor é: " + menor + " na posição: " + p);
                }
            }
        }
    }
}